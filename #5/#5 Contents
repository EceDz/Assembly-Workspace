Write an assembly language program that implements the following C code:
int x = 4;
int y = 5;
x = (x * 9) – (y * 7);
if (x > 6)
if (y > 7)
y = y * 2;
else
y = y / 2;
else
x = x / 4;
x = x + y;
Task Breakdown:
-. Initial values:
x = 4 should be stored at memory address $50.
y = 5 should be stored at memory address $60.
/. Mathematical Operations:
x = (x * 9) - (y * 7) should be performed using shift operations for multiplications.
Use conditional branches to check if x > 6 and y > 7.
0. Final Operations:
Depending on the result of the conditional checks, adjust the values of x and y using shift
operations for division and multiplication.
Add x and y, storing the result back in x.
1. Store x and y at memory addresses $50 and $60, respectively.
